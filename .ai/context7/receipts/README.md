# Context7 documentation receipts

This directory stores compact provenance/decision receipts for Context7 lookups used by Codex.

Do not store API keys, tokens, credentials, private source code or large copied documentation pages here.

Suggested file name:

`<task-id>-<technology>-<concept>.json`

Required metadata is defined in `.ai/context7-policy.yaml`.
