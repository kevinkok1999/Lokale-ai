# Uninstall

Uninstall is an explicit, reversible-aware operation. It stops services, preserves or exports user data by default, removes only owned components, records a report and requires confirmation before deleting state, credentials or backups.
