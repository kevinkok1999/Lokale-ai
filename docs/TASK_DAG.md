# Task DAG

A task has id, title, priority, dependencies, status, owner capability, risk, inputs, expected output, tests, acceptance criteria, evidence, blocker and next action. Independent tasks may run in parallel; dependent tasks wait. Leases, timeouts, idempotency keys and bounded retries make recovery deterministic.
